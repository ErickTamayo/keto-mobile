import { StyleSheet } from 'react-native'

const position = StyleSheet.create({
  absolute: {
    position: 'absolute',
  },
  pin: {
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
})

export default position

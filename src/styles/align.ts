import { StyleSheet } from 'react-native'

const items = StyleSheet.create({
  center: {
    alignItems: 'center',
  },
  flexStart: {
    alignItems: 'flex-start',
  },
  flexEnd: {
    alignItems: 'flex-end',
  },
  baseline: {
    alignItems: 'baseline',
  },
  stretch: {
    alignItems: 'stretch',
  },
})

export default {
  items,
}
